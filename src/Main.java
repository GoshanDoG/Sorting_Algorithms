import java.util.Arrays;
public class Main {
    public static void main(String args[]) {
     /* int[] sortArrFirst = {12, 6, 3, 0, 15, 9};
        int[] sortArrSecond = {0, 2, 4, 6, 10, 8};
        Bubble_Sort.bubbleSort(sortArrFirst);
        Bubble_Sort.bubbleSort(sortArrSecond);

        for (int i = 0; i < sortArrFirst.length; i++) {
            System.out.print(sortArrFirst[i] + " ");
        }

        System.out.println(" ");

        for (int i = 0; i < sortArrSecond.length; i++) {
            System.out.print(sortArrSecond[i] + " ");
        }
    */


//  < БЫСТРАЯ СОРТИРОВКА >

    /*  int[] sortArr = {12, 6, 4, 1, 15, 10};
        Quick_Sort.quickSort(sortArr, 0, sortArr.length - 1);
        for (int i = 0; i < sortArr.length; i++) {
            System.out.print(sortArr[i] + "\n");

    */

// < СОРТИРОВКА СЛИЯНИЕМ >
        /* public static int[] mergeSort(int[] sortArr) {
        int[] buffer1 = Arrays.copyOf(sortArr, sortArr.length);
        int[] buffer2 = new int[sortArr.length];
        int[] result = Merge_Sort.mergeSortInner(buffer1, buffer2, 0, sortArr.length);
        return result;
            }
      public static void main(String args[]) {
        int[] sortArr = {12, 6, 4, 1, 15, 10};
        int[] result = mergeSort(sortArr);
            System.out.println(Arrays.toString(result));

  */

// < СОРТИРОВКА СО ВСТАВКОЙ >

   /*   int[] sortArr = {12, 6, 4, 1, 15, 10};
        Insertation_Sort.insertionSort(sortArr);
        for (int i = 0; i < sortArr.length; i++) {
            System.out.print(sortArr[i] + "\n");

  */


        }
    }



